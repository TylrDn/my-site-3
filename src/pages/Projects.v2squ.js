
import { injectGlobalStyles } from 'public/globalStyles';
import { injectHtml } from 'public/injectHtml';


// ...existing code...
$w.onReady(function () {
  injectGlobalStyles();
  injectHtml('projectsHtml', 'projects');
});
            <strong>Outcome:</strong> 40% infra cost savings, 3× faster load times.
          </p>
        </div>
      </div>
    </div>
  `);
});
