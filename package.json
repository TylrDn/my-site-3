{
  "name": "macrosight-site",
  "version": "2.0.0",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test:e2e:ci": "node --test tests",
    "validate:links": "node scripts/validate-links.mjs",
    "test:a11y": "node scripts/test-a11y.mjs",
    "lint": "eslint -c .eslintrc.json .",
    "format": "prettier -w ."
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  }
}
